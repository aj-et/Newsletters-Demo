# Tools

Python scripts that do the actual work — API calls, data transforms, file ops.

Each script should:
- Accept inputs via CLI args or environment variables
- Print a clear result or error to stdout
- Exit 0 on success, non-zero on failure

Credentials come from `.env` in the project root.
