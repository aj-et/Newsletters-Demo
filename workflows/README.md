# Workflows

Markdown SOPs that define how each task gets done.

Each workflow should include:
- **Objective** — what this workflow produces
- **Inputs** — what's needed before starting
- **Steps** — which tools to run and in what order
- **Outputs** — what gets delivered and where
- **Edge cases** — known gotchas, rate limits, fallbacks
